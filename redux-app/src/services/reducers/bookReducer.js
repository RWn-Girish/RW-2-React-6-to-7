const initalState = {
    books: [],
    book: null
}

export const bookReducer = (state = initalState, action) => {
    switch (action.type) {
        case 1:
            
            break;
    
        default:
            break;
    }
}